spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/keepr_test
    username: keepr
    password: keepr_test
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: validate
